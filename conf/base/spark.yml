spark:
  spark_conf:
    spark.master: "local[*]"
    spark.app.name: "CreditCardPropensityPipeline"
    spark.executor.memory: "2g"
    spark.driver.memory: "2g"
    spark.sql.shuffle.partitions: "2"
